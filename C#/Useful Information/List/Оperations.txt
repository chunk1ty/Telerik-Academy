	List<int> list = new List<int> { 1, 2, 3 };     // int[] myArray = {1,2,3};
	list[0] = 1;                                    // myArray[0]=1;
	
	list.Remove(44);                                //Remove first found element !!
    
	list.Insert(0,4);                               // 1st - index after that the value that i want to enter //on index 0 i put 4
    
	list.InsertRange(0,listTwo);                    //insert more that one elemen // in this case i put new list (listTwo) from index 0 to length of listTwo 
    
	list.RemoveAt(1);                               //Remove by index //In this case i will remove 1 element (counting start from 0 !!!)
	
	list.RemoveAll(el => el == 4);                   // if el==4 remove 4 ;remove all elements who have value 4
        
	list.FindLastIndex(el => el == 4);                // found index of last variable with value 4
       
	bool check = list.TrueForAll(el => el == 4);      //return true if all elements are equal to 4
	
	list.All(el => el == 4);                           // equal to TrueForAll
	
	list.Last(el => el % 2 == 0);                     //found last element of the list who division on 2 without reminder
        
	list.FindAll(el => el == 4);                        // found all element who have value 4
    var listT = list.GetRange(1, 5);                    // return element from index 1 to index 5
	
	bool contan =list.Contains(4);                       //if in list i have 4 ->true else false
	
	var listThird = new List<int>(myArray);              // copy element from myArray to listThird (array->list)
    
	int[] array = list.ToArray();                              // (list->array)
        